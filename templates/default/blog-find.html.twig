{% extends 'base.html.twig' %}

{% block body %}
    <div class="example-wrapper">
        <h1>Blog find</h1>

        {% for blog in blogs %}
            <p>{{ blog.title }}</p>
            <p>{{ blog.description }}</p>
            <br>
        {% endfor %}

    </div>
{% endblock %}